doctype html
html(lang="zh-CN")
    meta(charset="utf-8")
    title 投票
    script(src="/js/ajax.js")
    script(src='/socket.io/socket.io.js')
    script.
        var award_list = new Array();
        $(function() {
            var socket = io();
            socket.on('danmu',function(data){
                console.log(data);
                var msg_obj = JSON.parse(data.msg);
            })
        })

        $(document).ready(function(){
            for(var i = 0; i < award_list.length; i++){
                $('div.total').append('<div class = candidate id = ' + i + '></div>');
                $('#id' + i).append('<div class = rank id = rank' + i + '>' + i + '</div>');
                $('#id' + i).append('<div class = name id = name' + i + '>' + msg_obj. + '</div>');
                $('#id' + i).append('<div class = vote id = vote' + i + '>' + vote + '</div>');  
            }
        })
    body
      .total
 
 style(type="text/css").
   div.total{
       positon:relative;
       left:10vw;
       width:80vw;
       height:100vh;
   }
   div.candidate{
       position:relative;
       width:100%;
       height:20%;
   }
   div.rank{
       position:relative;
       float:left;
       width:10%;
       height:100%;
   }
    div.profile{
       position:relative;
       float:left;
       width:50%;
       height:100%;
   }
   div.name{
       position:relative;
       float:left;
       width:30%;
       height:100%;
   }
   div.vote{
       position:relative;
       float:left;
       width:10%;
       height:100%;
   }